import React from 'react';
import { TITLE_APP } from '../../Constants/General';

const Title = () => (
    <div className="app-title">
        <h1>{TITLE_APP}</h1>
    </div>
);

export default Title;
